// import heroesReducer, { HeroesState } from "./heroesSlice"

// TODO: Add proper tests
// describe("heroes reducer", () => {
//   const initialState: HeroesState = {
//     value: 3,
//     status: "idle",
//   }
//   it("should handle initial state", () => {
//     expect(heroesReducer(undefined, { type: "unknown" })).toEqual({
//       value: 0,
//       status: "idle",
//     })
//   })
//   it("should handle increment", () => {
//     const actual = heroesReducer(initialState, increment())
//     expect(actual.value).toEqual(4)
//   })
//   it("should handle decrement", () => {
//     const actual = heroesReducer(initialState, decrement())
//     expect(actual.value).toEqual(2)
//   })
//   it("should handle incrementByAmount", () => {
//     const actual = heroesReducer(initialState, incrementByAmount(2))
//     expect(actual.value).toEqual(5)
//   })
// })
